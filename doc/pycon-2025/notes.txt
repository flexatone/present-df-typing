



# Elastic Generics: Flexible Static Typing with TypeVarTuple and Unpack



Since Python 3.5, generic tuples can be parameterized as either orderings of component types (e.g. `tuple[int, str]`) or unbound sequences of a single type (e.g. `tuple[float, ...]`). Yet sometimes we need both: for example, a `tuple` that starts with an `int` and a `str` and follows with zero or more `float`s might describe a row in a dataset that starts with identifiers and follows with a variable number of observations. While we might want to define a generic `Record` class that can be elastically parameterized in this manner, doing so was not practical until the introduction of `TypeVarTuple` and `Unpack` in Python 3.11.

With `TypeVarTuple`, a generic `Record` class can be made concrete as `Record[int, str]`, `Record[int, str, float]`, or even `Record[int, str, *tuple[float, ...]]`: the same class can be parameterized as requiring two types, three types, or required `int` and `str` types followed by zero or more `float` types (as given with the `Unpack` notation `*tuple[float, ...]`).

This presentation will introduce `TypeVarTuple` and `Unpack`. Starting with annotated `tuple` types, the flexible, expressive range of mixture between fixed and unbound types will be demonstrated with `mypy` validation. Next, a variadic generic class, `Record`, will demonstrate the benefits of such elastic typing.

Finally, a compelling application of `TypeVarTuple` will be demonstrated. While DataFrames are widely used, only the StaticFrame library, leveraging `TypeVarTuple`, offers a comprehensively generic DataFrame. DataFrames have variable numbers of columns, each sometimes a different type; further, while DataFrames might have tens or hundreds of columns, it is common for such datasets to have a fixed number of heterogeneously typed columns followed by a variable number of uniformly typed columns. With `TypeVarTuple`, this type of flexible DataFrame typing is now possible.





I. Introduction: 3 min
    The benefits of type hints
    Generics and generic tuples
        In the past, there were two options
            `tuple[int, str]`, `tuple[float, ...]`
        What if we want to combine both?
    `TypeVarTuple` and variadic generics
        Generic that permits parameterization with a sequence of types
        Can combine fixed types with an unbound region of a single type


II. Making variadic generics concrete: 5 min
    `tuple` supports `TypeVarTuple`-style parameterization
    Providing a fixed sequence of type parameters
        `row: tuple[int, str]`
        `row: tuple[int, str, float]`
        `row: tuple[int, str, *tuple[float, ...]]`
    Defining an unbound region of a single type
        From 3.12: `*tuple[float, ...]`
        For backwards compatibility: `Unpack[Tuple[float, ...]]`
        Using `typing-extensions` for `Unpack` compatibility before 3.11
    Combining fixed sequences with an unbound region `tuple` types
    Three options:
        `row: tuple[int, *tuple[float, ...]]`
        `row: tuple[*tuple[float, ...], str]`
        `row: tuple[int, *tuple[float, ...], str]`
        Mypy validation examples

III. Using `TypeVarTuple` to define generic classes: 7 min
    Data records in tabula data
        Commonly combine fixed identifiers with numerous observations
        An interface might support fixed identifiers variable columns of observations
        A variadic generic built with `TypeVarTuple` is a good fit
    Defining a class with TypeVarTuple
        In 3.12 new syntax permits `class Record[*Ts]: pass`
        Pre 3.12: `Ts = TypeVarTuple("Ts"); class Record(Generic[Ts]): pass`
    Examples of making a generic `Record` concrete
        `record: Record[int, str]`
        `record: Record[int, str, float]`
        `record: Record[int, str, *tuple[float, ...]]`
        Mypy validation examples
    Combining fixed generics with variadic generics
        `class Record[Ta, *Ts]`
        `class Record[*Ts, Tb]`
        `class Record[Ta, Tb, *Ts, Tc, Td]`


IV. Defining a generic DataFrame with `TypeVarTuple`: 10 min
    Many tabular datasets have strong expectations of starting columns
        A table with leading integers and string followed by variable numbers of floats
        The `Record` class above
    Using `TypeVarTuple` to define a `Table` class
        `class Table[Ts]: pass`
        `table: Table[int, str, *tuple[float, ...]]`
    DataFrames are more than tables
        Include typed index labels
        Include typed column labels
        Can include these as fixed type variables
    Defining a generic DataFrame
        Combine fixed with variadic generics
        `class Frame[TIndex, TColumns, Ts*]: pass`
        StaticFrame implementation
            Leverage generic `Index` type
            Leverage NumPy "generic" types in `Index` and columnar types
    Examples with Mypy validation:
        `frame: sf.Frame[sf.Index[np.str_], sf.Index[np.str_], np.str_, np.int64, *tuple[np.float64, ...]]`


V. Conclusion: 5 min
    Variadic generic are elastic
    `TypeVarTuple` challenges our notions of static typing
    How such types are expressive and flexible








